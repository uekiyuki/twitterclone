<%= form_with(model:  @heihei, local: true) do |form| %>
  <div class="heihei_content">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
  <%= link_to '投稿一覧画面にもどる', heiheis_path %>
<% end %>
